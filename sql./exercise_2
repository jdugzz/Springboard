-- SELECT * FROM owners JOIN vehicles ON owners.id = vehicles.owner_id;
-- SELECT first_name, last_name, COUNT(owner_id) FROM owners JOIN vehicles on owners.id = vehicles.owner_id GROUP BY (first_name, last_name) ORDER BY first_name;
-- SELECT first_name, last_name, ROUND(AVG(price)) as average_price, COUNT(owner_id) FROM owners JOIN vehicles ON owner.id = vehicles.owner_id GROUP BY (first_name, last_name) HAVING COUNT(owner_ID) > 1 AND average_price > 10000 ORDER BY first_name DESC;
